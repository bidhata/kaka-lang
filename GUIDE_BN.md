# কাকা ল্যাং (Kaka Lang) - সম্পূর্ণ কোডিং গাইড

**কাকা ল্যাং**-এ স্বাগতম, একটি মজার প্রোগ্রামিং ভাষা যা বাংলা স্ল্যাং এবং কথ্য ভাষার উপর ভিত্তি করে তৈরি। এই গাইড আপনাকে কাকা ল্যাং ডেভেলপার হওয়ার জন্য প্রয়োজনীয় সবকিছু শেখাবে!

## ১. শুরু করা

### ওয়েব কম্পাইলার (প্রস্তাবিত)
কাকা ল্যাং চালানোর সবচেয়ে সহজ উপায় হল বিল্ট-ইন ওয়েব কম্পাইলার ব্যবহার করা।
1. `web` ডিরেক্টরিতে যান।
2. `npm install` এবং `npm run dev` চালান।
3. আপনার ব্রাউজারে `http://localhost:5173` খুলুন।
4. কীওয়ার্ড সহজে টাইপ করতে **ভার্চুয়াল কীবোর্ড** ব্যবহার করুন!

### কমান্ড লাইন ইন্টারফেস (CLI)
আপনি Node.js ব্যবহার করে কাকা ল্যাং ফাইল (`.kaka`) চালাতে পারেন।
```bash
# একটি ফাইল চালান
npx ts-node src/index.ts your_file.kaka
```

---

## ২. প্রোগ্রাম স্ট্রাকচার

প্রতিটি কাকা ল্যাং প্রোগ্রাম অবশ্যই কাকাকে অভিবাদন দিয়ে শুরু হবে এবং বিদায় জানিয়ে শেষ হবে।

```javascript
ও কাকা
  // আপনার কোড এখানে লিখুন...
আসি কাকা
```

- **শুরু**: `ও কাকা` (ও কাকা / হাই আংকেল)
- **শেষ**: `আসি কাকা` (আসি কাকা / বাই আংকেল)
- **কমেন্ট**: সিঙ্গেল-লাইন কমেন্টের জন্য `//` ব্যবহার করুন।

---

## ৩. ভেরিয়েবল এবং ডেটা টাইপ

ভেরিয়েবল ডিক্লেয়ার করতে `কাকা রাখো` ব্যবহার করুন।

```javascript
কাকা রাখো name = "রাহুল";
কাকা রাখো age = 25;
কাকা রাখো isStudent = ঠিক; // true
কাকা রাখো job = ফালতু;     // null
```

### ডেটা টাইপ
- **সংখ্যা**: ইংরেজি (`123`) এবং বাংলা (`১২৩`) উভয় ডিজিট কাজ করে!
- **স্ট্রিং**: `"ডাবল কোট"` বা `'সিঙ্গেল কোট'`-এর ভিতরে টেক্সট।
- **বুলিয়ান**: `ঠিক` (True) এবং `ভুল` (False)।
- **নাল**: `ফালতু` (Useless/Null)।

---

## ৪. ইনপুট এবং আউটপুট

### প্রিন্টিং
`কাকা বলো` ব্যবহার করে কাকাকে কিছু বলতে বলুন।

```javascript
কাকা বলো "হ্যালো ওয়ার্ল্ড!";
কাকা বলো 10 + 20;
```

### ইনপুট
`কাকা শোনো` ব্যবহার করে কাকাকে শুনতে বলুন।

```javascript
কাকা রাখো name = কাকা শোনো;
কাকা বলো name;
```

---

## ৫. কন্ট্রোল ফ্লো

### If / Else
`যদি কাকা` এবং `নাহলে কাকা` দিয়ে সিদ্ধান্ত নিন।

```javascript
কাকা রাখো marks = 80;

যদি কাকা (marks > 33) {
   কাকা বলো "পাস করেছি!";
} নাহলে কাকা {
   কাকা বলো "ফেল!";
}
```

### লজিক্যাল অপারেটর
- **AND**: `এবং` -> উভয় সত্য হলে true রিটার্ন করে।
- **OR**: `অথবা` -> যেকোনো একটি সত্য হলে true রিটার্ন করে।

```javascript
যদি কাকা (age > 18 এবং hasID == ঠিক) {
   কাকা বলো "ভোট দিতে পারবে";
}
```

### লুপ (While)
`যতক্ষণ কাকা` দিয়ে কিছু পুনরাবৃত্তি করুন।

```javascript
কাকা রাখো i = 0;
যতক্ষণ কাকা (i < 5) {
   কাকা বলো i;
   i = i + 1;
}
```
- **Break**: `ব্যাস কাকা` (থামো)
- **Continue**: `পরেরটা কাকা` (পরেরটা)

---

## ৬. ফাংশন

`ফাংশন কাকা` ব্যবহার করে পুনরায় ব্যবহারযোগ্য কোড ব্লক তৈরি করুন। মান রিটার্ন করতে `ফেরত নাও` ব্যবহার করুন।

```javascript
ফাংশন কাকা jogfol(a, b) {
   ফেরত নাও a + b;
}

কাকা রাখো result = jogfol(10, 20);
কাকা বলো result; // 30
```

---

## ৭. অ্যারে (তালিকা)

একটি তালিকায় একাধিক মান সংরক্ষণ করুন। ইনডেক্স **0** থেকে শুরু হয়।

```javascript
কাকা রাখো numbers = [১০, ২০, ৩০];

// অ্যাক্সেস
কাকা বলো numbers[0]; // 10

// দৈর্ঘ্য
কাকা বলো numbers["length"]; // 3

// Push (শেষে যোগ করুন)
numbers["push"](40);

// Pop (শেষ থেকে সরান)
numbers["pop"]();
```

---

## ৮. উদাহরণ প্রজেক্ট: ফ্যাক্টোরিয়াল ক্যালকুলেটর

```javascript
ও কাকা
  ফাংশন কাকা factorial(n) {
     যদি কাকা (n == 0) {
        ফেরত নাও 1;
     }
     ফেরত নাও n * factorial(n - 1);
  }

  কাকা বলো "একটি সংখ্যা লিখুন:";
  কাকা রাখো num = কাকা শোনো;
  
  কাকা বলো factorial(num);
আসি কাকা
```

---

## ৯. ব্লক এডিটর (নতুন! 🧩)

কাকা ল্যাং-এ এখন একটি Scratch-এর মতো ভিজ্যুয়াল ব্লক এডিটর রয়েছে!

### বৈশিষ্ট্য:
- 🧩 ড্র্যাগ-এন-ড্রপ ব্লক ইন্টারফেস
- 🎨 ক্যাটাগরি অনুযায়ী রঙিন ব্লক
- 📋 কপি/পেস্ট এবং ডুপ্লিকেট ফিচার
- 🔄 টেক্সট কোড থেকে ব্লকে রূপান্তর
- 📝 ব্লক থেকে টেক্সট কোড দেখুন
- 🗑️ ওয়ার্কস্পেস ক্লিয়ার করুন

### ব্লক ক্যাটাগরি:
- 💬 **আউটপুট**: কাকা বলো
- 📦 **ভেরিয়েবল**: কাকা রাখো, পরিবর্তন করো
- 🎮 **কন্ট্রোল**: যদি কাকা, যতক্ষণ কাকা, পুনরাবৃত্তি
- ⌨️ **ইনপুট**: কাকা শোনো
- 🔢 **গণিত ও তুলনা**: +, -, ×, ÷, >, <, ==
- 🔗 **লজিক**: এবং, অথবা, নয়
- 💎 **মান**: সংখ্যা, টেক্সট, ঠিক, ভুল
- ⚙️ **ফাংশন**: ফাংশন কাকা, ফেরত নাও
- 📋 **তালিকা**: তৈরি, যোগ করো, নাও, দৈর্ঘ্য
- 💭 **অন্যান্য**: মন্তব্য, অপেক্ষা

### কীভাবে ব্যবহার করবেন:
1. ওয়েব ইন্টারফেস খুলুন
2. **Block Mode** বাটনে ক্লিক করুন
3. বাম দিক থেকে ব্লক টেনে মাঝখানে আনুন
4. ব্লকের প্যারামিটার পূরণ করুন
5. **▶ চালাও** বাটনে ক্লিক করুন
6. ডানদিকে আউটপুট দেখুন!

বিস্তারিত টিউটোরিয়ালের জন্য [Block Editor Tutorial](../examples/block_tutorial.md) দেখুন।

---

কাকা ল্যাং দিয়ে কোডিং উপভোগ করুন! 🎉
