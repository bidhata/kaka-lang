# Kaka Lang - Quick Reference Card

## Program Structure
```javascript
ও কাকা
  // Your code here
আসি কাকা
```

## Variables
```javascript
কাকা রাখো name = "রাহুল";    // Declare
name = "নতুন নাম";            // Assign
```

## Output & Input
```javascript
কাকা বলো "Hello!";           // Print
কাকা রাখো x = কাকা শোনো;     // Input
```

## Data Types
```javascript
কাকা রাখো num = 42;          // Number
কাকা রাখো text = "হাই";     // String
কাকা রাখো yes = ঠিক;         // Boolean true
কাকা রাখো no = ভুল;          // Boolean false
কাকা রাখো nothing = ফালতু;   // Null
```

## Conditionals
```javascript
যদি কাকা (x > 10) {
  কাকা বলো "বড়";
} নাহলে কাকা {
  কাকা বলো "ছোট";
}
```

## Loops
```javascript
যতক্ষণ কাকা (i < 5) {
  কাকা বলো i;
  i = i + 1;
}
```

## Loop Control
```javascript
ব্যাস কাকা;      // Break
পরেরটা কাকা;    // Continue
```

## Functions
```javascript
ফাংশন কাকা add(a, b) {
  ফেরত নাও a + b;
}

কাকা রাখো result = add(5, 3);
```

## Arrays
```javascript
কাকা রাখো arr = [1, 2, 3];
কাকা বলো arr[0];              // Access
arr["push"](4);                // Add
কাকা বলো arr["length"];       // Length
```

## Operators

### Arithmetic
```
+  -  *  /  %
```

### Comparison
```
>  <  >=  <=  ==  !=
```

### Logical
```javascript
x > 5 এবং y < 10    // AND
x == 0 অথবা y == 0  // OR
```

## Comments
```javascript
// Single line comment
```

## Keywords

| English | Kaka Lang | Usage |
|---------|-----------|-------|
| Start | ও কাকা | Program start |
| End | আসি কাকা | Program end |
| Print | কাকা বলো | Output |
| Declare | কাকা রাখো | Variable |
| Input | কাকা শোনো | Get input |
| If | যদি কাকা | Condition |
| Else | নাহলে কাকা | Alternative |
| While | যতক্ষণ কাকা | Loop |
| Break | ব্যাস কাকা | Exit loop |
| Continue | পরেরটা কাকা | Next iteration |
| Function | ফাংশন কাকা | Define function |
| Return | ফেরত নাও | Return value |
| And | এবং | Logical AND |
| Or | অথবা | Logical OR |
| True | ঠিক | Boolean true |
| False | ভুল | Boolean false |
| Null | ফালতু | Null value |

## Example Program
```javascript
ও কাকা
  ফাংশন কাকা greet(name) {
    কাকা বলো "হ্যালো " + name;
  }
  
  কাকা রাখো i = 0;
  যতক্ষণ কাকা (i < 3) {
    greet("বন্ধু");
    i = i + 1;
  }
আসি কাকা
```

---

**Print this for quick reference!** 📄
