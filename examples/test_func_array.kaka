
ও কাকা
  // ফাংশন এবং অ্যারে টেস্ট (Function and Array Test)

  // ১. ফাংশন (Function)
  ফাংশন কাকা যোগফল(a, b) {
     ফেরত নাও a + b;
  }

  কাকা বলো "১০ আর ২০ এর যোগফল:";
  কাকা রাখো result = যোগফল(১০, ২০);
  কাকা বলো result;

  // ২. অ্যারে (Array)
  কাকা রাখো লিস্ট = [১, ২, ৩, ৪, ৫];
  কাকা বলো "লিস্ট এর ৩য় আইটেম:";
  কাকা বলো লিস্ট[2]; // Index starts at 0

  // ৩. অ্যারে মেথড (Array Methods)
  কাকা বলো "লিস্ট এর দৈর্ঘ্য (Length):";
  কাকা বলো লিস্ট["length"];

  // Typo fix: List spelling
  লিস্ট["push"](৬);
  কাকা বলো "নতুন আইটেম ঢোকানোর পর:";
  কাকা বলো লিস্ট[5];

  // ৪. রিকারসন ফাংশন (Recursion)
  ফাংশন কাকা ফ্যাক্টরিয়াল(n) {
     যদি কাকা (n == 0) {
        ফেরত নাও ১;
     }
     ফেরত নাও n * ফ্যাক্টরিয়াল(n - 1);
  }

  কাকা বলো "৫ এর ফ্যাক্টরিয়াল:";
  কাকা বলো ফ্যাক্টরিয়াল(৫);

আসি কাকা
